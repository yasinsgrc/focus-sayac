<?xml version="1.0" encoding="utf-8"?>
<!--
  Hizli Odak widget (4x2). Ust satir sinav ozeti, altta tek eylem butonu.
  Butonun kendi dokunma niyeti var: govde geri sayimi, buton odagi aciyor.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_quick_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_surface"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="12dp"
    android:paddingEnd="16dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/widget_quick_kicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/michroma_regular"
        android:letterSpacing="0.26"
        android:text="@string/widget_target"
        android:textColor="@color/focus_accent_400"
        android:textSize="8sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:baselineAligned="false"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_quick_exam"
            android:text="@string/widget_pick_exam"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="@font/inter_600"
            android:maxLines="1"
            android:textColor="@color/focus_text"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/widget_quick_days"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:fontFamily="@font/space_grotesk_700"
            android:maxLines="1"
            android:textColor="@color/focus_neutral_300"
            android:textSize="14sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_quick_button"
        android:text="@string/widget_focus_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="@drawable/widget_button"
        android:fontFamily="@font/space_grotesk_700"
        android:gravity="center"
        android:letterSpacing="0.08"
        android:maxLines="1"
        android:paddingTop="11dp"
        android:paddingBottom="11dp"
        android:textColor="@color/focus_bg"
        android:textSize="13sp" />
</LinearLayout>
