<?xml version="1.0" encoding="utf-8"?>
<!--
  Seri widget (2x2). Ust: SERI kicker + gun sayisi. Orta: bugunku odak.
  Alt: son 7 gunun mini sutunlari (SparkRenderer) ve sinav ozeti.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_streak_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_surface"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_streak_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/michroma_regular"
        android:letterSpacing="0.26"
        android:text="@string/widget_streak_tag"
        android:textColor="@color/focus_ember"
        android:textSize="8sp" />

    <TextView
        android:id="@+id/widget_streak_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:fontFamily="@font/space_grotesk_700"
        android:maxLines="1"
        android:textColor="@color/focus_text"
        android:textSize="26sp" />

    <TextView
        android:id="@+id/widget_streak_today"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:fontFamily="@font/inter_500"
        android:maxLines="1"
        android:textColor="@color/focus_neutral_400"
        android:textSize="11sp" />

    <ImageView
        android:id="@+id/widget_streak_spark"
        android:src="@drawable/widget_preview_spark"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1"
        android:contentDescription="@string/widget_streak_label"
        android:scaleType="fitXY" />

    <TextView
        android:id="@+id/widget_streak_exam"
        android:text="@string/widget_pick_exam"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:fontFamily="@font/inter_500"
        android:maxLines="1"
        android:textColor="@color/focus_neutral_500"
        android:textSize="10sp" />
</LinearLayout>
